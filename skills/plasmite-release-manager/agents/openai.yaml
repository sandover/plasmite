interface:
  display_name: "Plasmite Release Manager"
  short_description: "Run fail-closed split release workflows"
  default_prompt: "Use $plasmite-release-manager to run a fail-closed Plasmite release with QA gates, provenance checks, and publish-only rerun support."
